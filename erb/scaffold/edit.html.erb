<h1><%%= t("helpers.actions.edit", model: <%= class_name %>.model_name.human) %></h1>

<%%= render 'form' %>

<div class="form-actions">
	<%%= link_to t("helpers.actions.show"), @<%= singular_table_name %>, class: 'btn' %> 
	<%%= link_to t("helpers.links.back"), <%= plural_table_name %>_path, class: 'btn' %>
</div>
